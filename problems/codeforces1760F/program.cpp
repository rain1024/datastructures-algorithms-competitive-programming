#include <bits/stdc++.h>
#include <vector>

using namespace std;

int main(){
  ios::sync_with_stdio(false);
  cin.tie(0);
  int tt;
  cin >> tt;
  long long c, d;
  int n;
  while(tt--){
    cin >> n >> c >> d;
    vector<int> a;
    int ai;
    for(int i=0; i<n; i++){
      cin >> ai;
      a.push_back(ai);
    }
    cout << 0 << '\n';
  }
  return 0;
}